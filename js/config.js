/* Configuration */
const config = [];

/* Defaults */
const defaults = [];

defaults.viewWidth = 500;
defaults.viewHeight = 500;
defaults.viewResize = true;
defaults.viewX = 100;
defaults.viewY = 100;