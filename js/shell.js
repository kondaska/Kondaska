/* Kondaska Shell */
const shell = [];

// Shell log
shell.log = function(msg) {
    console.log(msg);
};

// Shell warn
shell.warn = function(msg) {
    console.error(msg);
};

// Shell error
shell.error = function(msg) {
    console.error(msg);
};
